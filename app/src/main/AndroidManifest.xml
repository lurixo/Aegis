<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Aegis">

        <activity
            android:name=".ui.SetupActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|smallestScreenSize|keyboardHidden|uiMode|density|navigation|locale|layoutDirection|fontScale"
            android:theme="@style/Theme.Aegis"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.InputSettingsActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|smallestScreenSize|keyboardHidden|uiMode|density|navigation|locale|layoutDirection|fontScale"
            android:label="@string/settings_group_input_title"
            android:theme="@style/Theme.Aegis"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".ui.DictSettingsActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|smallestScreenSize|keyboardHidden|uiMode|density|navigation|locale|layoutDirection|fontScale"
            android:label="@string/settings_group_dicts_title"
            android:theme="@style/Theme.Aegis"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".ui.UserDictActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|smallestScreenSize|keyboardHidden|uiMode|density|navigation|locale|layoutDirection|fontScale"
            android:label="@string/settings_group_userdict_title"
            android:theme="@style/Theme.Aegis"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".ui.AboutActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|smallestScreenSize|keyboardHidden|uiMode|density|navigation|locale|layoutDirection|fontScale"
            android:label="@string/settings_group_about_title"
            android:theme="@style/Theme.Aegis"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".ui.LicensesActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|smallestScreenSize|keyboardHidden|uiMode|density|navigation|locale|layoutDirection|fontScale"
            android:label="@string/settings_about_licenses_title"
            android:theme="@style/Theme.Aegis"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.PhraseTransferActivity"
            android:autoRemoveFromRecents="true"
            android:exported="false"
            android:excludeFromRecents="true"
            android:finishOnTaskLaunch="true"
            android:noHistory="false"
            android:taskAffinity=""
            android:theme="@style/Theme.Aegis.Transparent" />

        <service
            android:name=".AegisInputMethodService"
            android:exported="true"
            android:label="@string/ime_label"
            android:permission="android.permission.BIND_INPUT_METHOD">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

    </application>

</manifest>
